import hyphen from 'hyphen/fr';

/** Debounce duration for all resize events across the application */
export const RESIZE_DEBOUNCE = 100;

/** Default document title template for Meta component */
export const DOCUMENT_TITLE = (title: string) => title;

export { hyphen };
